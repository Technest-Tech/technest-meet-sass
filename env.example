# Database (PostgreSQL)
# Example: postgresql://user:password@localhost:5432/almajd_meet
DATABASE_URL="postgresql://user:password@localhost:5432/almajd_meet"

# Super Admin (for initial setup)
SUPER_ADMIN_EMAIL="admin@almajd.com"
SUPER_ADMIN_PASSWORD="admin123"

# JWT Authentication
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# LiveKit Configuration
LIVEKIT_API_KEY="devkey"
LIVEKIT_API_SECRET="secret"
# For local development with Docker, use your local IP address (not localhost)
# Find your IP with: ipconfig getifaddr en0 (macOS) or ip addr (Linux)
# This should match the advertise_ip in livekit.yaml
LIVEKIT_URL="ws://192.168.1.23:7880"

# File storage (point to an absolute path on the server for persistence)
ROOM_UPLOAD_ROOT="/var/lib/almajd/uploads"

# Cloudflare R2 Storage Configuration
# R2 is used for storing room file uploads. If R2 is enabled and configured,
# new uploads will be stored in R2. Existing files remain on local filesystem.
# If R2 upload fails, the system will automatically fallback to local storage.
R2_ACCESS_KEY_ID="7cc88bb0e07b172a19b37cdec4512970"
R2_SECRET_ACCESS_KEY="2863ad840ac813b4a496f8b065c353ccee4a088e07a40975d31344414ca24ea3"
R2_ENDPOINT="https://3eb7ab379add240613a41c5ea0a1e9e5.r2.cloudflarestorage.com"
R2_BUCKET_NAME="your-bucket-name"
# Set to "false" to disable R2 and use local storage only (defaults to true if not set)
R2_ENABLED="true"

# Base URL used in generated links (set to your deployed domain)
APP_URL="http://localhost:3000"

# Optional path that referral links should open (example: /subscription-request)
# Leave empty to use the root path
REFERRAL_TARGET_PATH="/subscription-request"

# Next.js
# For local development with Docker, use your local IP address (not localhost)
# This should match LIVEKIT_URL and advertise_ip in livekit.yaml
NEXT_PUBLIC_LIVEKIT_URL="ws://192.168.1.23:7880"
NEXT_PUBLIC_LK_RECORD_ENDPOINT="/api/record"
NEXT_PUBLIC_SHOW_SETTINGS_MENU="true"
